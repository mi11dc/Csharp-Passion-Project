@model Csharp_Passion_Project.Models.TeamDto
@{
    ViewBag.Title = "Team Details";
}

<h2>@ViewData["title"]</h2>

<nav>
    <p>
        @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-primary" })
        @Html.ActionLink("Edit", "Edit", new { id = @Model.Id }, new { @class = "btn btn-warning" })
        @Html.ActionLink("Delete", "ConfirmDelete", new { id = @Model.Id }, new { @class = "btn btn-danger" })
    </p>
</nav>

<div>
    <dl class="dl-horizontal">
        <dt>
            Team Id
        </dt>
        <dd>
            @Model.Id
        </dd>

        <dt>
            Team Name
        </dt>
        <dd>
            @Model.Name
        </dd>

        <dt>
            Owner Name
        </dt>
        <dd>
            @Model.Owner
        </dd>

        <dt>
            Formed On
        </dt>
        <dd>
            @Model.FormedOn.ToShortDateString()
        </dd>

    </dl>
</div>

@*<div class="row">
    <div class="col-12">
        <table border="1">
            <thead>
                <tr>
                    <th>#</th>
                    <th>Player Name</th>
                    <th>DOB</th>
                    <th>Country</th>
                    <th>BasePrice</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Count() == 0)
                {
                    <tr class="text-center">
                        <td colspan="6">No Data Found</td>
                    </tr>
                }
                @{ int i = 1; }
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@i</td>
                        <td>@item.FName&nbsp;@item.LName</td>
                        <td>@item.DOB.ToShortDateString()</td>
                        <td>@item.Country</td>
                        <td>@item.BasePrice</td>
                        <td>
                            @Html.ActionLink("View", "Details", new { id = @item.Id }, new { @class = "btn btn-primary" })
                            @Html.ActionLink("Edit", "Edit", new { id = @item.Id }, new { @class = "btn btn-warning" })
                            @Html.ActionLink("Delete", "ConfirmDelete", new { id = @item.Id }, new { @class = "btn btn-danger" })
                        </td>
                    </tr>

                    i++;
                }
            </tbody>
        </table>
    </div>
</div>*@
